// Single active academic session for the whole school. The Fees module is
// scoped to one session at a time, so this is a plain constant rather than a
// settings screen nobody asked for.
export const CURRENT_ACADEMIC_SESSION = '2026-27';
